🔎 Issues in Your Code
1. Artificial Delay (isLayoutStable)
setTimeout(() => setIsLayoutStable(true), 2000);


Snap scrolling (scrollSnapType) is disabled for 2s, then suddenly enabled.

During those 2s, the browser lets you scroll freely → then forces snap → "jump."

2. Spline Background (TechFestBackground)

Even though you set position: fixed, Spline still repaints & resizes the canvas for a frame when it loads, which can shift scroll position once.

This happens especially with overflow-y-scroll containers.

3. Mobile vh problem
height: 'calc(100dvh - 4rem)'


✅ You’re already using dvh which is good.
But in your hero section you still have:

minHeight: 'calc(100dvh - 4rem)',
height: 'calc(100dvh - 4rem)',


If minHeight and height both fight each other, Safari sometimes flickers the scroll area when address bar shows/hides.

4. Framer Motion animations on containers

Example:

<motion.div initial={{ opacity: 0, x: 30, scale: 0.9 }} whileInView={{ opacity: 1, x: 0, scale: 1 }} />


Animating scale on full-page snap sections causes the element’s height to change during animation → the browser recalculates snap → jump.

5. Fonts not stabilized

Your gradient TECHFEST’25 uses a custom font.
Until the font loads, the browser renders fallback → size mismatch → pushes layout → snap recalculates.

✅ Fixes You Can Apply
🔧 Immediate Fixes

Remove artificial snap delay

scrollSnapType: filteredEvents.length > 0 ? 'y mandatory' : 'none',


→ Get rid of isLayoutStable. If you want a loading overlay, keep it, but don’t delay snap activation.

Lock Spline Background Size
In TechFestBackground:

<div className="fixed inset-0 w-full h-full -z-10">
  <canvas
    ref={canvasRef}
    className="w-full h-full"
    style={{
      background: 'transparent',
      position: 'absolute',
      inset: 0,
      width: '100%',
      height: '100%',
      pointerEvents: 'none'
    }}
  />
</div>


→ Prevents a single-frame resize when Spline loads.

Avoid animating container scale
Change:

initial={{ opacity: 0, x: 30, scale: 0.9 }}
whileInView={{ opacity: 1, x: 0, scale: 1 }}


to:

initial={{ opacity: 0, x: 30 }}
whileInView={{ opacity: 1, x: 0 }}


→ Keep scale: 1 always.
If you want subtle zoom, apply it only to child content, not the full snap section.

Font Preload
Add this in _document.tsx:

<link
  rel="preload"
  href="/fonts/techfont.woff2"
  as="font"
  type="font/woff2"
  crossOrigin="anonymous"
/>


→ Ensures TECHFEST’25 doesn’t reflow after load.

Simplify Hero Section Sizing
Replace:

minHeight: 'calc(100dvh - 4rem)',
height: 'calc(100dvh - 4rem)',


with:

height: 'calc(100dvh - 4rem)',


→ Don’t mix both on mobile.